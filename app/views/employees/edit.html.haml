%h1.m-t-2.m-b-1.text-xs-center
  = @client.company.name
  %small.text-muted
    = @client.name
    = link_to fa_icon("pencil"), [:edit, @client.company, @client]

.row
  .col-md-5
    .list-group
      = render @client.company.clients
  .col-md-6
    %h2.text-muted.text-xs-center
      Editar expediente
      %small.text-muted= @employee
    = render "form"
