%nav.navbar.navbar-light.bg-faded
  = link_to "Bufete Adm. Corporativa", companies_path, class: "navbar-brand"

  %form.form-inline.pull-left
    %input.form-control{placeholder: "Buscar Cliente", type: "text", id: "client-search"}

  %ul.nav.navbar-nav.pull-right
    - if logged_in?
      %a.nav-item.nav-link.active.text-muted
        = fa_icon("user", text: current_user.email)
      %li.nav-item
        = link_to "Cerrar sesión", sign_out_path, method: :delete, class: "nav-link"
        %span.sr-only (current)
